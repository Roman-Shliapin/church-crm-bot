# 🔒 Безпека проєкту

Цей документ описує заходи безпеки, впроваджені для захисту персональних даних користувачів.

## 🛡️ Реалізовані заходи безпеки

### 1. Захист конфіденційних даних

- ✅ **Токен бота** зберігається в `.env` файлі (не в коді)
- ✅ **ID адміністраторів** зберігаються в `.env` файлі
- ✅ **JSON файли з даними** додані в `.gitignore` (не комітяться в git)
- ✅ **.env файл** додані в `.gitignore`

### 2. Валідація вхідних даних

Усі дані від користувачів проходять валідацію перед збереженням:

- ✅ **Імена** - перевірка формату, довжини (2-100 символів), дозволені символи
- ✅ **Телефони** - валідація українського формату (+380XXXXXXXXX)
- ✅ **Дата хрещення** - перевірка формату (ДД-ММ-РРРР) та валідності дати
- ✅ **Текстові поля** - санітизація та обмеження довжини (до 5000 символів)

### 3. Санітизація даних

- ✅ Видалення потенційно небезпечних символів (HTML теги)
- ✅ Обрізання пробілів
- ✅ Перевірка на порожні рядки

### 4. Rate Limiting

- ✅ Обмеження кількості повідомлень: **20 на хвилину** на користувача
- ✅ Захист від спаму та DDoS атак
- ✅ Автоматичне очищення старих записів

### 5. Контроль доступу

- ✅ Команди для адмінів захищені middleware `checkAdmin`
- ✅ Перевірка прав перед виконанням адмінівських команд

### 6. Обмеження розміру даних

- ✅ Текстові поля обмежені до 5000 символів
- ✅ Оголошення обмежені до 4000 символів
- ✅ Захист від витрати пам'яті та переповнення файлів

## 📋 Налаштування безпеки

### Створення .env файлу

Створіть файл `.env` в корені проєкту:

```env
BOT_TOKEN=ваш_токен_бота_з_BotFather
ADMIN_IDS=580788346,1015055588
```

**⚠️ ВАЖЛИВО:**
- Ніколи не комітьте `.env` файл в git!
- Не діліться токеном бота з іншими
- Зберігайте `.env` в безпечному місці

### Перевірка прав доступу до файлів

Переконайтеся, що JSON файли з даними мають правильні права доступу:

```bash
chmod 600 members.json needs.json prayers.json
```

## 🔍 Моніторинг та логування

✅ **Впроваджено систему логування:**

- ✅ Логування всіх запитів до бота
- ✅ Логування спроб доступу до адмінівських команд
- ✅ Логування перевищення rate limit
- ✅ Логування помилок читання/запису файлів
- ✅ Логування успішних операцій
- ✅ Автоматичне очищення старих логів (старіші за 30 днів)

**Логи зберігаються в:**
- `logs/bot-YYYY-MM-DD.log` - всі події
- `logs/errors-YYYY-MM-DD.log` - помилки та попередження

**Приклади логування:**
- Підозрілі дії (спроби неавторизованого доступу)
- Перевищення rate limit
- Помилки валідації даних
- Помилки збереження/читання файлів

## ⚠️ Важливі зауваження

1. **Шифрування**: Поточна реалізація не використовує шифрування даних на диску. Для виробничого середовища рекомендовано:
   - Використовувати базу даних з шифруванням (PostgreSQL, MongoDB з шифруванням)
   - Або додати шифрування файлів (crypto модуль Node.js)

2. **Резервне копіювання**: Регулярно створюйте резервні копії JSON файлів з даними

3. **Оновлення**: Регулярно оновлюйте залежності (`npm audit`)

4. **Моніторинг**: Спостерігайте за розміром файлів та кількістю запитів

## 🚀 Рекомендації для виробництва

Для production середовища рекомендовано:

- [x] ✅ Налаштувати логування - **ВПРОВАДЖЕНО**
- [ ] Використовувати базу даних (PostgreSQL, MongoDB) замість JSON файлів
- [ ] Додати шифрування даних на диску (crypto модуль Node.js)
- [ ] Додати моніторинг (Prometheus, Grafana)
- [ ] Використовувати HTTPS для API (якщо буде)
- [ ] Регулярно робити резервні копії
- [ ] Налаштувати автоматичне оновлення залежностей
- [ ] Налаштувати алерти при помилках (email, Telegram)

