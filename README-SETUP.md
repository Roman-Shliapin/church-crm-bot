# 🚀 Швидкий старт

## Перші кроки

### 1. Створення .env файлу

У вас є два варіанти:

#### Варіант А: Автоматичне створення (рекомендовано)

Запустіть інтерактивний скрипт:

```bash
npm run setup
```

Або:

```bash
node scripts/setup-env.js
```

Скрипт попросить ввести:
- `BOT_TOKEN` - токен бота з @BotFather
- `ADMIN_IDS` - ID адміністраторів через кому (наприклад: `580788346,1015055588`)

#### Варіант Б: Ручне створення

Створіть файл `.env` в корені проєкту:

```env
BOT_TOKEN=ваш_токен_бота_з_BotFather
ADMIN_IDS=580788346,1015055588
```

### 2. Встановлення залежностей

Якщо ще не встановлено:

```bash
npm install
```

### 3. Запуск бота

```bash
npm start
```

Або:

```bash
node bot.js
```

## 📁 Структура файлів

```
church-crm-bot/
├── .env                 # ⚠️ Створіть цей файл з токеном та ADMIN_IDS
├── bot.js              # Головний файл
├── members.json        # Дані членів (створюється автоматично)
├── needs.json          # Заявки на допомогу (створюється автоматично)
├── prayers.json        # Молитвенні потреби (створюється автоматично)
├── logs/               # Логи (створюється автоматично)
│   ├── bot-YYYY-MM-DD.log
│   └── errors-YYYY-MM-DD.log
└── ...
```

## ✅ Перевірка налаштувань

Після запуску бота перевірте:

1. ✅ Бот запустився без помилок
2. ✅ Створено директорію `logs/` з файлами логів
3. ✅ JSON файли створюються автоматично (якщо їх немає)
4. ✅ Бот відповідає на команду `/start`

## 🔒 Безпека

⚠️ **ВАЖЛИВО:**

- Ніколи не комітьте `.env` файл в git
- Не діліться токеном бота з іншими
- JSON файли з даними автоматично ігноруються git
- Логи також ігноруються git

## 📖 Команди бота

Після запуску використовуйте команди:

- `/start` - почати роботу
- `/help` - список команд
- `/register` - реєстрація
- `/me` - ваш профіль
- `/need` - подати заявку
- `/pray` - молитвенна потреба
- `/lessons` - біблійні уроки
- `/contact` - контакти

Для адмінів додатково:
- `/members` - список членів
- `/needs` - заявки
- `/prayers` - молитви
- `/announce` - оголошення

## 🐛 Проблеми?

### Бот не запускається

1. Перевірте, чи створено `.env` файл
2. Перевірте правильність `BOT_TOKEN`
3. Перевірте права доступу до файлів
4. Перегляньте логи в `logs/errors-*.log`

### Помилка "BOT_TOKEN не встановлено"

Створіть файл `.env` з правильним токеном (див. вище).

### Дані не зберігаються

Перевірте права доступу до директорії:
```bash
ls -la *.json
```

Якщо потрібно, надайте права:
```bash
chmod 644 *.json
```

## 📚 Додаткова документація

- `SECURITY.md` - деталі безпеки
- `STRUCTURE.md` - структура проєкту
- `README.md` - повна документація

