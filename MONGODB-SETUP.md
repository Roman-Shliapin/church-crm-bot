# 🗄️ Налаштування MongoDB Atlas

Цей посібник допоможе вам налаштувати MongoDB Atlas для бота.

## 📋 Крок 1: Створення акаунту MongoDB Atlas

1. Перейдіть на [mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas)
2. Натисніть **"Sign Up"** або **"Try Free"**
3. Заповніть форму реєстрації
4. Підтвердіть email

## 📋 Крок 2: Створення кластера

1. Після входу натисніть **"Build a Database"**
2. Оберіть **"FREE"** (M0) - безкоштовний варіант
3. Оберіть хмарного провайдера та регіон (найближчий до вас)
4. Введіть назву кластера (наприклад: `ChurchCRM`)
5. Натисніть **"Create"**

⏳ Створення кластера займе кілька хвилин

## 📋 Крок 3: Створення користувача бази даних

1. Після створення кластера вам запропонують створити користувача
2. Введіть **Username** (наприклад: `church-crm-user`)
3. Введіть **Password** (обов'язково збережіть його!)
4. Натисніть **"Create Database User"**

💡 **Важливо:** Збережіть username та password! Вони будуть потрібні для підключення.

## 📋 Крок 4: Дозвіл доступу (Network Access)

1. В меню зліва оберіть **"Network Access"**
2. Натисніть **"Add IP Address"**
3. Для розробки натисніть **"Allow Access from Anywhere"** (0.0.0.0/0)
   - ⚠️ Для production оберіть конкретні IP адреси
4. Натисніть **"Confirm"**

## 📋 Крок 5: Отримання Connection String

1. В меню зліва оберіть **"Database"**
2. Натисніть **"Connect"** на вашому кластері
3. Оберіть **"Connect your application"**
4. Оберіть **"Node.js"** та версію **"6.0 or later"**
5. Скопіюйте connection string (виглядає як: `mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority`)

## 📋 Крок 6: Додавання до .env файлу

1. Відкрийте `.env` файл у вашому проекті
2. Додайте connection string:

```env
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=580788346,1015055588
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/church-crm?retryWrites=true&w=majority
```

**Важливо:**
- Замініть `<username>` на ваш username з кроку 3
- Замініть `<password>` на ваш password з кроку 3
- Замініть `cluster0.xxxxx.mongodb.net` на ваш cluster адрес
- `church-crm` - це назва бази даних (можна змінити на будь-яку)

## 📋 Крок 7: Встановлення залежностей

В терміналі виконайте:

```bash
npm install
```

Це встановить MongoDB драйвер.

## ✅ Готово!

Після налаштування MongoDB Atlas та додавання `MONGODB_URI` до `.env`, бот автоматично підключиться до бази даних при запуску.

## 🔍 Перевірка роботи

1. Запустіть бота: `npm start`
2. Перевірте логи - має з'явитися повідомлення про успішне підключення до MongoDB
3. Створіть тестову реєстрацію через `/register`
4. Перевірте в MongoDB Atlas - дані мають з'явитися в колекції `members`

## 📊 Структура колекцій

Бот автоматично створить такі колекції в MongoDB:

- **members** - члени церкви
- **needs** - заявки на допомогу
- **prayers** - молитвенні потреби
- **lessons** - біблійні уроки

## 🔒 Безпека

✅ **Ніколи не комітьте `.env` файл в Git!**  
✅ Використовуйте сильні паролі для бази даних  
✅ Для production обмежте Network Access конкретними IP адресами  
✅ Регулярно створюйте резервні копії бази даних

## 💡 Поради

- Безкоштовний tier (M0) має обмеження 512 MB, але цього достатньо для початку
- MongoDB Atlas автоматично робить резервні копії
- Можна переглядати дані в браузері через MongoDB Atlas UI

## 🆘 Проблеми?

**Помилка підключення:**
- Перевірте, чи правильно скопійовано connection string
- Перевірте, чи додано ваш IP до Network Access
- Перевірте, чи правильно введено username та password

**Дані не зберігаються:**
- Перевірте логи на наявність помилок
- Перевірте, чи правильно налаштовано `MONGODB_URI`

