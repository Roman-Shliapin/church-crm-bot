# ✅ Як перевірити, що MongoDB працює

## 🔍 Перевірка 1: Логи при запуску

Після запуску бота (`npm start`) ви повинні побачити в логах:

```
✅ Підключено до MongoDB
✅ Bot запущено і він слухає команди...
```

Якщо бачите помилку типу:
```
❌ Помилка підключення до MongoDB: ...
```

Перевірте:
- Чи правильно скопійовано `MONGODB_URI` з MongoDB Atlas
- Чи правильно введено username та password
- Чи додано ваш IP до Network Access в MongoDB Atlas

## 🔍 Перевірка 2: Створення тестової реєстрації

1. Відкрийте бота в Telegram
2. Виконайте команду `/register`
3. Заповніть дані реєстрації
4. Після успішної реєстрації ви побачите: `✅ Дякуємо, [ім'я]! Ви успішно зареєстровані.`

Якщо все добре, дані збережені в MongoDB!

## 🔍 Перевірка 3: Перегляд в MongoDB Atlas

1. Відкрийте [MongoDB Atlas](https://cloud.mongodb.com)
2. Перейдіть до вашого кластера
3. Натисніть **"Browse Collections"**
4. Ви повинні побачити базу даних `church-crm` (або назву, яку ви вказали)
5. Всередині повинні бути колекції:
   - `members` - члени церкви
   - `needs` - заявки на допомогу
   - `prayers` - молитвенні потреби
   - `lessons` - біблійні уроки

## 🔍 Перевірка 4: Перегляд даних

Після реєстрації:

1. Відкрийте колекцію `members` в MongoDB Atlas
2. Ви повинні побачити документ з вашими даними:
   ```json
   {
     "id": 123456789,
     "name": "Ваше ім'я",
     "baptism": "01-01-2020",
     "birthday": "15-03-1990",
     "phone": "+380..."
   }
   ```

## ✅ Що означає, що все працює:

- ✅ Бот запускається без помилок
- ✅ В логах є повідомлення "Підключено до MongoDB"
- ✅ Реєстрація проходить успішно
- ✅ Дані з'являються в MongoDB Atlas
- ✅ JSON файли більше НЕ оновлюються (вони тепер не використовуються)

## 🔄 Що відбувається під капотом:

1. **При старті бота:**
   - Бот підключається до MongoDB Atlas
   - Створюється підключення до бази даних

2. **При реєстрації користувача:**
   - Дані зберігаються в колекцію `members` в MongoDB
   - JSON файли більше не використовуються

3. **При створенні заявки на допомогу:**
   - Дані зберігаються в колекцію `needs` в MongoDB

4. **При додаванні молитвенної потреби:**
   - Дані зберігаються в колекцію `prayers` в MongoDB

5. **При завантаженні уроку:**
   - Дані зберігаються в колекцію `lessons` в MongoDB

## 💡 Порада

Якщо хочете переконатися, що дані точно в MongoDB, а не в файлах:

1. Видаліть або перейменуйте JSON файли (`members.json`, `needs.json`, тощо)
2. Запустіть бота
3. Створіть нову реєстрацію
4. Перевірте MongoDB Atlas - дані повинні з'явитися
5. Перевірте, що JSON файли НЕ оновлюються

---

**Важливо:** JSON файли тепер не використовуються, але їх можна залишити як резервну копію або видалити.

