# 📤 Як запушити код на GitHub

Цей посібник допоможе вам опублікувати ваш проект на GitHub.

## 📋 Крок 1: Створіть репозиторій на GitHub

1. Перейдіть на [github.com](https://github.com) і увійдіть у свій акаунт
2. Натисніть кнопку **"+"** (праворуч угорі) → **"New repository"**
3. Введіть назву репозиторію (наприклад: `church-crm-bot`)
4. Оберіть **Private** (приватний) або **Public** (публічний)
5. **НЕ** ставте галочки на "Add README", "Add .gitignore" та "Choose a license" (у нас вже є файли)
6. Натисніть **"Create repository"**

## 📋 Крок 2: Ініціалізуйте Git у вашому проекті

Відкрийте термінал у папці проекту та виконайте команди:

```bash
# Ініціалізуйте Git репозиторій
git init

# Додайте всі файли до staging area
git add .

# Зробіть перший коміт
git commit -m "Initial commit: Church CRM Bot"
```

## 📋 Крок 3: Підключіть віддалений репозиторій

На сторінці створеного репозиторію GitHub ви побачите команди. Виконайте:

```bash
# Замініть YOUR_USERNAME та YOUR_REPOSITORY на ваші дані
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git

# Перевірте, що все правильно
git remote -v
```

**АБО якщо використовуєте SSH:**

```bash
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPOSITORY.git
```

## 📋 Крок 4: Запуште код на GitHub

```bash
# Запуште код на GitHub (перший раз)
git branch -M main
git push -u origin main
```

Якщо це приватний репозиторій, GitHub попросить вас ввести логін та пароль (або токен доступу).

## ✅ Готово!

Після цього ваш код буде доступний на GitHub. Ви зможете переглянути його в браузері.

---

## 🔄 Для майбутніх змін

Коли ви внесете зміни в код, виконайте:

```bash
# Перевірте, які файли змінилися
git status

# Додайте змінені файли
git add .

# Створіть коміт з описом змін
git commit -m "Опис того, що ви змінили"

# Запуште зміни на GitHub
git push
```

## 🔒 Важливо про безпеку

Перед тим, як пушити код, переконайтеся, що:

✅ Файл `.env` додано до `.gitignore` (не комітиться)  
✅ Файли `members.json`, `needs.json`, `prayers.json` додано до `.gitignore`  
✅ Логи та Excel файли не комітуються  

Всі ці файли вже є в `.gitignore`, тому вони автоматично не будуть включені в репозиторій.

## 💡 Поради

- **Змістовні коміти**: Пишіть зрозумілі повідомлення комітів, щоб потім легко зрозуміти історію змін
- **Регулярні коміти**: Робіть коміти часто, а не один великий в кінці
- **Перед пушем**: Завжди перевіряйте `git status`, щоб переконатися, що ви пушите те, що потрібно

