# 🚀 Деплой на Render

Цей посібник допоможе вам задеплоїти бота на Render.

## 📋 Передумови

1. ✅ Код завантажено на GitHub
2. ✅ MongoDB Atlas налаштовано та працює
3. ✅ `.env` файл містить всі необхідні змінні

## 📋 Крок 1: Створення Web Service на Render

1. Перейдіть на [render.com](https://render.com) та увійдіть в акаунт
2. Натисніть **"New +"** → **"Web Service"**
3. Підключіть ваш GitHub репозиторій:
   - Якщо ще не підключено: **"Connect account"** → авторизуйте GitHub
   - Оберіть репозиторій з вашим ботом
4. Заповніть форму:
   - **Name:** `church-crm-bot` (або будь-яка назва)
   - **Region:** оберіть найближчий регіон (наприклад, Frankfurt, Germany)
   - **Branch:** `main` (або `master`, залежно від вашої гілки)
   - **Root Directory:** залиште порожнім (якщо проект в корені)
   - **Runtime:** `Node`
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`

## 📋 Крок 2: Налаштування Environment Variables

У розділі **"Environment Variables"** додайте всі змінні з вашого `.env` файлу:

### Обов'язкові змінні:

```
BOT_TOKEN=ваш_токен_бота
ADMIN_IDS=580788346,1015055588
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/church-crm?retryWrites=true&w=majority
```

### Як додати:

1. Натисніть **"Add Environment Variable"**
2. Введіть **Key** (наприклад: `BOT_TOKEN`)
3. Введіть **Value** (ваш токен)
4. Натисніть **"Save Changes"**
5. Повторіть для всіх змінних

⚠️ **Важливо:** НЕ додавайте `.env` файл в GitHub! Використовуйте тільки Environment Variables в Render.

## 📋 Крок 3: Налаштування плану

1. Оберіть **"Free"** план (безкоштовний)
2. Переконайтеся, що **"Auto-Deploy"** увімкнено (автоматичний деплой при push)

## 📋 Крок 4: Деплой

1. Натисніть **"Create Web Service"**
2. Render почне деплой автоматично
3. Переглядайте логи в реальному часі

## 🔄 Як зробити ручний деплой (Manual Deploy)

Якщо ви хочете зробити деплой вручну без push в GitHub:

### Варіант 1: Через Render Dashboard

1. Відкрийте ваш Web Service на Render
2. Перейдіть на вкладку **"Manual Deploy"**
3. Оберіть гілку (наприклад, `main`)
4. Натисніть **"Deploy latest commit"**
5. Чекайте завершення деплою

### Варіант 2: Через GitHub (рекомендовано)

1. Зробіть commit ваших змін:
   ```bash
   git add .
   git commit -m "Оновлення коду"
   ```

2. Запуште зміни на GitHub:
   ```bash
   git push origin main
   ```

3. Render автоматично почне деплой (якщо Auto-Deploy увімкнено)

4. Якщо Auto-Deploy вимкнено:
   - Перейдіть на Render Dashboard
   - Відкрийте ваш Web Service
   - Натисніть **"Manual Deploy"** → **"Deploy latest commit"**

### Варіант 3: Через Render CLI (для досвідчених)

1. Встановіть Render CLI:
   ```bash
   npm install -g render-cli
   ```

2. Авторизуйтеся:
   ```bash
   render login
   ```

3. Задеплойте:
   ```bash
   render deploy
   ```

## 🔍 Перевірка деплою

### Перевірка логів:

1. Відкрийте ваш Web Service на Render
2. Перейдіть на вкладку **"Logs"**
3. Перевірте логи на наявність помилок

### Очікувані логи при успішному деплої:

```
✅ Підключено до MongoDB
✅ Bot запущено і він слухає команди...
```

### Типові помилки:

**❌ "MONGODB_URI не встановлено"**
- Перевірте, чи додано `MONGODB_URI` в Environment Variables

**❌ "BOT_TOKEN не встановлено"**
- Перевірте, чи додано `BOT_TOKEN` в Environment Variables

**❌ Помилка підключення до MongoDB**
- Перевірте `MONGODB_URI` (правильний пароль, username)
- Перевірте Network Access в MongoDB Atlas (додайте IP Render)

## 🔧 Налаштування Network Access в MongoDB Atlas

Render використовує динамічні IP адреси. Для безкоштовного tier MongoDB Atlas:

1. Відкрийте MongoDB Atlas → **Network Access**
2. Додайте IP адресу: `0.0.0.0/0` (дозволити з будь-якого IP)
   - ⚠️ Це безпечно тільки якщо ви використовуєте сильний пароль

Або краще:
1. Додайте IP адресу вашого Render сервісу (знайдете в Render Dashboard)

## 📊 Статуси деплою

- **Building** - білдиться
- **Live** - працює
- **Deploy failed** - помилка деплою (перевірте логи)

## 🔄 Оновлення коду

Після внесення змін у код:

1. Зробіть commit:
   ```bash
   git add .
   git commit -m "Опис змін"
   ```

2. Запуште на GitHub:
   ```bash
   git push origin main
   ```

3. Render автоматично задеплоїть нову версію

Або використайте Manual Deploy через Dashboard.

## 💡 Поради

✅ Завжди перевіряйте логи після деплою  
✅ Використовуйте Environment Variables замість `.env` файлу  
✅ Не комітьте `.env` файл в GitHub  
✅ Перевірте, що всі залежності в `package.json`  
✅ Для production обмежте Network Access в MongoDB Atlas  

## 🆘 Проблеми?

**Бот не запускається:**
- Перевірте логи в Render Dashboard
- Перевірте Environment Variables
- Перевірте `package.json` та `start` скрипт

**Дані не зберігаються:**
- Перевірте підключення до MongoDB (логи)
- Перевірте `MONGODB_URI` в Environment Variables
- Перевірте Network Access в MongoDB Atlas

**Автоматичний деплой не працює:**
- Перевірте, чи увімкнено "Auto-Deploy"
- Перевірте гілку (має відповідати гілці в GitHub)

---

**Готово!** Ваш бот тепер доступний 24/7 на Render! 🎉

